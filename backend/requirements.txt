# Runtime dependencies for the Vercel backend service (entrypoint: backend/main.py).
#
# This file is used ONLY by Vercel's Python builder. Local dev, CI, and Docker
# install from pyproject.toml instead: pip install -e ".[dev]"
#
# Intentionally excluded from the serverless bundle:
#   - chromadb / onnxruntime: pushes the function past Vercel's 250 MB limit,
#     and /tmp-persisted vectors don't survive between invocations anyway.
#     app/ai/rag.py degrades gracefully when chromadb is absent (RAG disabled).
#   - dev tooling (pytest, ruff, mypy, coverage): not needed at runtime.

# --- Web framework ---
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9

# --- Database ---
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0

# --- AI / LLM (Google AI Studio) ---
google-genai>=1.0.0
pdfminer.six>=20231228
PyMuPDF>=1.24.0

# --- Auth & security ---
PyJWT>=2.8.0
bcrypt>=4.0.0
python-dotenv>=1.0.0

# --- Configuration ---
pydantic>=2.7.0
pydantic-settings>=2.2.0
email-validator>=2.1.0

# --- Logging ---
structlog>=24.1.0
